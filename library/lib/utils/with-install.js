const t = (c, o) => {
  if (c.install = (n) => {
    for (const s of [c, ...Object.values(o != null ? o : {})])
      n.component(s.name, s);
  }, o)
    for (const [n, s] of Object.entries(o))
      c[n] = s;
  return c;
};
export {
  t as default,
  t as withInstall
};
